{
  "_comment": "Copy this file to jobs.config.local.json (gitignored) and edit for your machine. It deep-merges over jobs.config.json: objects merge key-by-key, scalars and arrays are replaced wholesale, your values win. This is where your repos live — the base config ships none. The repo dropdown's '+ Add repo' writes here too, so you can start from the UI and hand-edit later.",

  "repos": {
    "my-repo": { "path": "C:\\path\\to\\your\\repo", "trunk": "main" }
  },

  "_example_port_override": { "port": 7800 }
}
